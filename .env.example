# LLM keys
OPENAI_API_KEY=sk‑xxx
MISTRAL_API_KEY=hf_yyy

# where your MCP geocoder server is running
MCP_URL=http://localhost:8000/mcp/      # or https://example.com/mcp/
MCP_LABEL=default                       # must match the server label

# default options (can still be overridden on the CLI)
GEONEXT_PROVIDER=openai                 # or mistral
GEONEXT_FLUSH=1                         # flush to disk after every doc
LOG_LEVEL=INFO
LOG_FILE=geonext.log
